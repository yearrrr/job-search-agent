# 首次交付检查

日期：2026-09-13。范围是本次文档与环境准备，**不是应用自动化测试，也不是 A01–A15 功能验收**。

| 检查 | 结果 | 证据与边界 |
| --- | --- | --- |
| 原始需求副本 | 通过 | 与用户附件逐字节一致；SHA-256 见下方 |
| 需求编号 | 通过 | 整理稿含 F1–F6 六节及 A01–A15 十五条验收行，无缺号或重复行；完整原文另存供复核 |
| 文档本地链接 | 通过 | 在最终报告落盘后检查所有 Markdown 相对文件链接，目标均存在 |
| 原扩展保护 | 通过 | 原有 23 个文件的目录聚合摘要与初始一致 |
| 原压缩包保护 | 通过 | SHA-256 与初始一致 |
| Git 忽略规则 | 通过 | 11 个应忽略路径、5 个应保留路径，使用实际 Git 规则检查 |
| 临时检查目录 | 已清理 | 临时 Git 元数据仅用于忽略规则验证；项目本身仍未初始化 Git |
| Python / Git 版本 | 已检测 | 详见环境检查记录 |
| SQLite 内存基础读写 / FTS5 英文查询 | 通过 | 不代表磁盘持久化、中文召回或业务测试通过 |
| Web 应用与 LangGraph 业务图 | 未实现/未测试 | 后续按阶段计划开发 |
| 模拟模型业务测试 | 未建立/未执行 | 不能标记“模拟测试通过” |
| DeepSeek API 及真实样本 | 未执行 | 真实模型请求数为 0；没有实测 Token 或金额记录 |
| 独立环境安装与启动 | 未执行 | A15 未验收 |

原始需求 SHA-256：`e9f826f2c8cd0eeb275391ca41038b5fa42759ffea9ecb82586578b376fbda59`。已有目录与压缩包摘要保存在[环境检查记录](environment-check.md)。

Git 忽略检查使用临时仓库元数据并指定新项目为工作树，通过 `git check-ignore --no-index -z --stdin` 检查示例路径，不创建真实隐私样本。覆盖 `.env` 及本地变体、原始简历目录、检查点、导出材料、私有评测、日志、数据库 WAL、虚拟环境和缓存；确认 `.env.example`、README、需求文档、虚构样例路径、`pyproject.toml` 不被这些规则屏蔽。

检查中首次脚本因 Windows 文本管道把换行转换为 CRLF，使 Git 将回车误当作文件名内容，断言失败。随后改用字节输入与 NUL 分隔，16 个路径案例全部通过；没有为通过检查而修改忽略规则。该问题属于一次性检查脚本，不是应用故障测试记录。

本次只做文档/环境的一次性检查，没有为文档生成应用测试套件。后续应在实际业务代码存在后建立有意义的自动化测试，并把结果、失败案例和真实验收记录单独保存。
